import Head from "next/head";

const Meta = () => {
  return (
    <Head>
      <meta name="description" content="temperature monitoring record display"/>
      <meta
        name="viewport"
        content="minimum-scale=1, initial-scale=1, width=device-width"
      />
    </Head>
  )
}